# sandboxed.sh Workspace

This is a **host workspace** managed by sandboxed.sh.

- Use the built-in `Bash` tool to run shell commands directly
- Skills are available in `.claude/skills/` - use `/help` to list them
